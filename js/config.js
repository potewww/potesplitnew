// ============================================================
// config.js — MODIFICA QUESTI VALORI prima di pubblicare la home
// ============================================================
const CONFIG_HOME = {
  owner: "potewww",   // dove vive QUESTA repo (home), es. "potewww"
  repo: "potesplitnew",          // nome di questa repo, es. "prixpartager-home"
  branch: "main",                 // di solito "main"
  org: "potesplitnew"  // organization dove verranno create le repo degli eventi
};
